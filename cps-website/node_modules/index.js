const sqlite3 = required(sqlite3).verbose();
const db = new sqlite3.Database('./cps.db', sqlite3.OPEN_READWRITE, (err)=>{
    if (err) return console.log(err.message);
    console.log("connection succefful")
})

alert("successful?")